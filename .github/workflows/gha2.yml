name : gha_practice
on : workflow_dispatch
jobs :
    build :
      runs-on : ubuntu-latest
      steps :
          - name : step1
          run : echo "step1"
    test:
      runs-on : ubuntu-latest
      steps :
          - name : test
          run : echo "test"
    deploy:
      runs-on : ubuntu-latest
      steps :
          - name : deploy
          run : echo "deploy"
